{% import '@SyliusUi/Macro/buttons.html.twig' as buttons %}

{% set path = options.link.url|default(path(options.link.route)) %}
{% if quick_return(data)  %}
{{ buttons.default(path, action.label, null, 'redo', 'yellow' ) }}
{% endif %}
